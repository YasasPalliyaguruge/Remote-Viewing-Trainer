# Development only. The current Vite build exposes this value to the browser.
# Use a restricted key and do not use this client-side architecture for a public deployment.
GEMINI_API_KEY=replace_with_a_restricted_development_key
